apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-service-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/H8rsh100/ForgeOps.git
    targetRevision: HEAD
    path: charts/api-service
    helm:
      valueFiles:
        - ../../gitops/dev/api-service-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: forgeops-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
